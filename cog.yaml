# Configuration for Cog 
build:
  gpu: false
  python_version: "3.11"
  run:
    - "apt-get update -qq && apt-get install -qqy ffmpeg libsndfile1 pkg-config libffi-dev && rm -rf /var/lib/apt/lists/*"
  python_requirements: "requirements.txt"

predict: "predict.py:Predictor"
