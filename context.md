2026-05-31 01:45

# 프로젝트 컨텍스트: 오토파일럿 고도화 및 랜딩 페이지 성능/디자인 전면 개편

## 1. 개요
오토파일럿 플랫폼 고도화와 함께, 고정 모델 구독에서 크레딧 기반 유연 모델 선택 구조로의 전환을 진행 중. 최근 랜딩 페이지의 'AI 템플릿 느낌(AI Slop)'을 제거하기 위한 디자인 리팩토링과 Lighthouse 성능 점수(TBT, CLS)를 극대화하기 위한 고강도 엔진 튜닝을 병행하고 있음.

## 2. 완료된 작업

### 2.1~2.5 (기존 작업 내역 유지)
- 오토파일럿 플랫폼 관리 업그레이드, AI 모델 데이터 정제, 관리자 API 구축 완료.

### 2.6 랜딩 페이지 비용 계산기 최적화 - [완료]
- `CostStructureSection` 컴포넌트의 렌더링 성능 최적화(memoization) 완료.

### 2.7 랜딩 페이지 디자인 개편 (Phase 1: Hero, Features, Comparison) - [진행 중]
- **디자인 컨셉 전환**: 'Asymmetric(비대칭)' 레이아웃을 도입하여 프리미엄 브랜드 룩 구현.
- **Hero 섹션**: 'Overlapping Depth' 레이아웃 적용. 회전 효과를 제거하고 메인 영상 뒤에 서브 영상을 정갈하게 배치하여 시선 집중도 향상.
- **Features 섹션**: 9:16 비디오 비율을 보존하는 'Staggered Grid'로 변경하여 영상 잘림 문제 해결.
- **Comparison 섹션**: 가짜 터미널 UI를 제거하고 Editorial 스타일의 텍스트 하이라이팅 시스템으로 개편.

### 2.8 비디오 리소스 및 고강도 성능 최적화 - [진행 중]
- **TBT(차단 시간) 문제 해결**: `ComparisonSection`의 60fps 리렌더링 루프를 제거하고 **'직접 DOM 제어(Ref)'** 방식으로 전환하여 메인 스레드 부하 최소화.
- **Single DOM 아키텍처 설계**: 반응형 레이아웃 전환 시 비디오 태그가 삭제/재생성되는 문제를 해결하기 위해 단 3개의 태그를 고정하고 CSS로만 배치를 바꾸는 구조 적용.
- **환경 변수 통일**: 모든 비디오 소스를 `NEXT_PUBLIC_DEMO_ASSETS_URL` 기반의 클라우드 주소로 정규화.
- **에러 수정**: 존재하지 않는 `grid.svg` 참조 제거 및 Docker 환경의 `next.config.ts` 모듈 로딩 이슈 해결.

## 3. 향후 작업 (Next Steps) - [Priority: HIGH]

1. **비디오 재생 복구 및 최종 안정화**:
   - `next-video` 라이브러리의 원격 URL 처리 한계를 극복하기 위해 최적화된 표준 `<video>` 태그 기반 엔진으로 복구하거나 Vidstack 등 대안 검토.
   
2. **랜딩 페이지 하단 섹션 디자인 개편**:
   - `HowItWorks`, `Pricing`, `FAQ` 섹션의 AI 슬롭(그라데이션 텍스트, 대칭 그리드 등)을 걷어내고 비대칭 레이아웃 적용.

3. **AI 모델 DB Import 및 UI 연동**:
   - `initial_ai_model_list.csv` 데이터의 실제 DB 반영 및 오토파일럿/수동 2-Track 전략 스키마 구축.

4. **크레딧 및 과금 구조 개편 (Phase 3)**:
   - Polar.sh 크레딧 상품 셋업 및 자동 충전 로직 구현.
