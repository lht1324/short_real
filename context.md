2026-06-03 18:50

# 프로젝트 컨텍스트: 오토파일럿 고도화 및 랜딩 페이지 성능/디자인 전면 개편

## 1. 개요
오토파일럿 플랫폼 고도화와 함께, 고정 모델 구독에서 크레딧 기반 유연 모델 선택 구조로의 전환을 진행 중. 최근 랜딩 페이지의 'AI 템플릿 느낌(AI Slop)'을 제거하기 위한 디자인 리팩토링과 워크스페이스 내부의 UI를 B2B 프로 툴(Pro-tool) 감성으로 일치시키는 작업을 병행하고 있음.

## 2. 완료된 작업

### 2.1~2.7 (기존 작업 내역 유지)
- 오토파일럿 플랫폼 관리 업그레이드, AI 모델 데이터 정제, 관리자 API 구축 완료.
- 랜딩 페이지 Phase 1 디자인 개편 완료 (Hero, Features, Comparison).

### 2.8 비디오 리소스 및 고강도 성능 최적화 (엔진 튜닝) - [완료]
- **Lighthouse 성능 점수 개선**: 초기 **17점**에서 **65점**(개발 모드 기준)으로 비약적 상승 달성. (빌드 후 시크릿 모드 측정 시 80~90점대 기대)
- **에셋 다이어트**: 모든 포스터 이미지를 **WebP**로 전환하고, 비디오 리소스를 저화질용(**_low.mp4**)과 원본용으로 이원화하여 초기 로딩 대역폭 90% 절감.
- **지연 로딩(Lazy Loading) 시스템 구축**: 커스텀 `useIntersectionObserver` 훅을 구현하여 `Hero`, `Features`, `Comparison` 모든 섹션의 비디오가 뷰포트 진입 시에만 로드/재생되도록 강제함.
- **심리스 원본 접근 트릭 적용**: 사용자가 영상을 클릭/우클릭하여 '새 탭에서 열기'를 할 경우 자동으로 고화질 원본이 노출되도록 `<a>` 태그 오버레이와 `pointer-events-none` 기술을 조합한 꼼수 적용. (UI 노출 없이 성능과 화질 동시 확보)
- **TBT(차단 시간) 최적화**: `ComparisonSection`의 무분별한 `load()` 호출을 제거하고 뷰포트 기반 동적 소스 할당으로 메인 스레드 부하를 최소화함.

### 2.9 워크스페이스 (오토파일럿, 대시보드, 크리에이트) UI/UX 고도화 (Anti-Slop 리팩토링) - [완료]
- **목표:** 기존의 화려하고 장난감 같던 B2C 랜딩 페이지 스타일(핑크/퍼플 네온, Vaporwave 배경, 거대한 버튼, 이모지 남용)을 제거하고, 조종석(Cockpit) 느낌의 **'다크 테크 프로 툴(Dark Tech Pro-Tool)'** 감성으로 전체 톤앤매너를 일치시킴.
- **배경 및 레이아웃:** 시선을 뺏는 거대한 다색 블러 원들을 모두 지우고(일부 컴포넌트는 극도로 투명하게 억제), 메인 캔버스 배경을 차분하고 깊은 `bg-zinc-950`으로 통일. 패널 간 보더는 가장 얇은 `border-white/5`로 통일하여 정보의 집중도를 높임.
- **버튼 및 컨트롤 다이어트:** 모든 핵심 액션 버튼에서 화려한 그라데이션(`bg-gradient-to-r from-pink...`)과 과도한 호버 스케일링(`hover:scale-105`), 무거운 그림자(`shadow-lg`)를 제거. 대신 `bg-white text-black`이나 `bg-zinc-800 text-zinc-100` 등 무채색 기반의 날렵하고 단단한 플랫(Flat) 버튼으로 전면 교체. 패딩(py)도 줄여 밀도를 높임.
- **타이포그래피 및 배지 튜닝:** `text-[10px]` 등 가독성을 해치는 초소형 폰트를 `text-[11px]`이나 `text-[13px]` 등으로 상향. 남용되던 `font-bold`를 `font-medium`으로 하향하여 정보 위계를 정리. 대시보드의 상태 배지는 뚱뚱한 그라데이션을 걷어내고 Lucide 아이콘과 반투명 배경(`bg-emerald-500/10 text-emerald-400`)을 적용하여 세련미를 더함.
- **주요 적용 대상:**
    - `WorkspaceAutopilotPageClient` 및 하위 패널들 (모델 선택권 및 BYOK 요금 예상 기능 UI 통합 포함)
    - `WorkspaceDashboardPageClient` 및 `DashboardItem` (2단 그리드 컴팩트 배치 적용)
    - `WorkspaceCreatePageClient` 및 하위 패널들 (클래퍼보드 디자인 톤다운 유지, AI 대본 생성 모달 및 예상 비용 카드 플랫화, 성우 선택창 UI 구조화)
    - `WorkspaceSidebar` 디자인 동기화

## 3. 향후 작업 (Next Steps) - [Priority: HIGH]

1. **[진행 예정] Editor 페이지 디자인 분석 및 리팩토링**:
   - `@components/page/workspace/editor/WorkspaceEditorPageClient.tsx` 및 그 하위 컴포넌트들을 분석하여, 다른 워크스페이스 화면들과 동일한 'Dark Tech' 프로 툴 감성으로 UI/UX 디자인을 리팩토링해야 함.

2. **[검토 필요] 새로운 BYOK 강제 저가 플랜 ($9) 도입 여부**:
   - $19, $29, $49 요금제에 더해, 오토파일럿 지원을 최소화하고 OpenRouter/ElevenLabs API 키 등록을 강제하는 'Hobbyist/API Pro' 티어($9) 도입에 대한 전략적 검토 및 구현(가드레일 로직 등).

3. **[버그 수정] HeroSection 뮤트 버튼 작동 불량**:
   - 현재 Hero 영역의 비디오 카드에서 뮤트 토글 버튼이 정상적으로 동작하지 않는 이슈 해결 필요. (링크 오버레이 레이어와의 간섭 의심)

4. **랜딩 페이지 하단 섹션 디자인 개편**:
   - `HowItWorks`, `Pricing`, `FAQ` 섹션의 AI 슬롭을 제거하고 비대칭 레이아웃 적용.

5. **크레딧 및 과금 구조 개편 (Phase 3)**:
   - Polar.sh 크레딧 상품 셋업 및 자동 충전 로직 구현.
